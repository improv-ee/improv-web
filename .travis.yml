language: php

php:
  - 7.2

before_script:
  - cp src/.env.testing src/.env
  - touch src/storage/framework/testing/db.sqlite

install:
  - composer install -d src --prefer-source --no-interaction --dev

script:
  - php src/artisan migrate
  - php src/vendor/bin/phpunit
